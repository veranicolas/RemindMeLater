import React from 'react';
import { View, SafeAreaView, Text, StyleSheet } from 'react-native'

const Main = () =>{

    return (
        <SafeAreaView>
            <View>
                <Text>Hola si nene</Text>
            </View>
        </SafeAreaView>
    );
};

const styles = StyleSheet.create({

});

export { Main }